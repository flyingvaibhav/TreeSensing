# 🌳 TreeSense Architecture Diagram

## System Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                     http://localhost:3000                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP Requests
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      NEXT.JS FRONTEND                            │
├─────────────────────────────────────────────────────────────────┤
│  Pages:                                                          │
│  • / (page.tsx)           → Landing Page                         │
│  • /upload (page.tsx)     → Upload & Detect                      │
│  • /trees (page.tsx)      → Detection History                    │
│                                                                   │
│  Utils:                                                          │
│  • utils/api.ts           → API Functions                        │
│  • lib/config.ts          → Axios Setup                          │
│                                                                   │
│  Constants:                                                      │
│  • constants/types.ts     → TypeScript Interfaces                │
│  • constants/config.ts    → API Endpoints                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ REST API Calls
                            │ POST /upload
                            │ GET /detections
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FASTAPI BACKEND                             │
│                   http://127.0.0.1:8000                          │
├─────────────────────────────────────────────────────────────────┤
│  Endpoints:                                                      │
│  • POST /upload           → Upload image for detection           │
│  • GET /detections        → Get all detection records            │
│  • GET /detections/{id}   → Get specific detection               │
│  • GET /health            → Health check                         │
│                                                                   │
│  Features:                                                       │
│  • CORS enabled for frontend                                     │
│  • File validation (size, type)                                  │
│  • Error handling with HTTPException                             │
│  • MongoDB integration                                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ Image Processing
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    YOLO ML MODEL                                 │
│                    models/best.pt                                │
├─────────────────────────────────────────────────────────────────┤
│  • Model: YOLOv8 (Ultralytics)                                   │
│  • Task: Object Detection                                        │
│  • Class: Tree (single class)                                    │
│  • Confidence Threshold: 0.25                                    │
│  • Input: PIL Image                                              │
│  • Output: Bounding boxes + confidence scores                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ Detection Results
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     MONGODB DATABASE                             │
│                   Database: tree_sense                           │
├─────────────────────────────────────────────────────────────────┤
│  Collection: detections                                          │
│  {                                                               │
│    "filename": "image.jpg",                                      │
│    "tree_count": 45,                                             │
│    "avg_confidence": 0.87,                                       │
│    "confidences": [0.92, 0.85, ...],                             │
│    "image_size": {                                               │
│      "width": 1920,                                              │
│      "height": 1080                                              │
│    },                                                            │
│    "timestamp": "2025-10-30T12:34:56"                            │
│  }                                                               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ Query Results
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BACK TO FRONTEND                            │
│              Display Results & History                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow - Upload & Detect

```
User Selects Image
       ↓
Frontend Validates (size, type)
       ↓
Create FormData with file
       ↓
POST /upload
       ↓
Backend receives UploadFile
       ↓
Read image bytes
       ↓
Open with PIL
       ↓
Load YOLO model (if not loaded)
       ↓
Run inference: model(image, conf=0.25)
       ↓
Extract results:
  • boxes = results[0].boxes
  • tree_count = len(boxes)
  • confidences = boxes.conf.tolist()
       ↓
Calculate avg_confidence
       ↓
Prepare detection_data object
       ↓
Insert into MongoDB
  db.detections.insert_one(detection_data)
       ↓
Return JSON response
       ↓
Frontend receives response
       ↓
Display results to user:
  • Tree count
  • Confidence scores
  • Image details
  • Timestamp
```

---

## File Upload Flow

```
┌──────────────┐
│   Browser    │
│ <input file> │
└──────┬───────┘
       │
       │ File selected
       ▼
┌──────────────────┐
│  File Validation │
│  • Max 10MB      │
│  • JPG/PNG only  │
└──────┬───────────┘
       │
       │ Valid
       ▼
┌──────────────────┐
│  Create Preview  │
│  FileReader API  │
└──────┬───────────┘
       │
       │ User clicks "Detect"
       ▼
┌──────────────────┐
│  FormData        │
│  append('file')  │
└──────┬───────────┘
       │
       │ axios.post()
       ▼
┌──────────────────┐
│  Backend         │
│  Receives file   │
└──────────────────┘
```

---

## Component Hierarchy

```
App
├── Layout (layout.tsx)
│   └── Global styles
│
├── Home Page (/)
│   ├── Hero Section
│   ├── Action Cards
│   │   ├── Upload Card → Link to /upload
│   │   └── History Card → Link to /trees
│   ├── Features Section
│   └── Tech Stack
│
├── Upload Page (/upload)
│   ├── File Input
│   ├── Image Preview
│   ├── Upload Button
│   ├── Loading State
│   ├── Error Message
│   └── Results Display
│       ├── Tree Count Card
│       ├── Confidence Card
│       └── Details Section
│
└── History Page (/trees)
    ├── Stats Dashboard
    │   ├── Total Detections
    │   ├── Total Trees
    │   └── Avg Confidence
    ├── Action Button (New Detection)
    └── Results Table
        ├── Filename
        ├── Tree Count
        ├── Confidence
        ├── Image Size
        └── Timestamp
```

---

## API Request/Response Examples

### POST /upload

**Request:**
```http
POST http://127.0.0.1:8000/upload
Content-Type: multipart/form-data

file: aerial_image.jpg
```

**Response:**
```json
{
  "success": true,
  "message": "Detected 45 trees",
  "data": {
    "filename": "aerial_image.jpg",
    "tree_count": 45,
    "avg_confidence": 0.87,
    "confidences": [0.92, 0.85, 0.90, 0.88, ...],
    "image_size": {
      "width": 1920,
      "height": 1080
    },
    "timestamp": "2025-10-30T12:34:56.789",
    "id": "507f1f77bcf86cd799439011"
  }
}
```

### GET /detections

**Request:**
```http
GET http://127.0.0.1:8000/detections?limit=10
```

**Response:**
```json
{
  "success": true,
  "count": 10,
  "data": [
    {
      "filename": "image1.jpg",
      "tree_count": 45,
      "avg_confidence": 0.87,
      "timestamp": "2025-10-30T12:34:56"
    },
    ...
  ]
}
```

---

## Technology Stack Layers

```
┌─────────────────────────────────────┐
│         PRESENTATION LAYER          │
│  Next.js 14 + React 18 + TypeScript │
│         Tailwind CSS                │
└─────────────┬───────────────────────┘
              │
┌─────────────▼───────────────────────┐
│         APPLICATION LAYER           │
│    Custom Hooks + API Functions     │
│         Axios HTTP Client           │
└─────────────┬───────────────────────┘
              │
┌─────────────▼───────────────────────┐
│            API LAYER                │
│  FastAPI + RESTful Endpoints        │
│         CORS Middleware             │
└─────────────┬───────────────────────┘
              │
┌─────────────▼───────────────────────┐
│         BUSINESS LOGIC              │
│   Image Processing + Validation     │
│      YOLO Model Integration         │
└─────────────┬───────────────────────┘
              │
┌─────────────▼───────────────────────┐
│         DATA ACCESS LAYER           │
│    PyMongo + MongoDB Queries        │
└─────────────┬───────────────────────┘
              │
┌─────────────▼───────────────────────┐
│         PERSISTENCE LAYER           │
│    MongoDB Database (tree_sense)    │
│     Collection: detections          │
└─────────────────────────────────────┘
```

---

## Security & Validation

```
Frontend Validation:
├── File Size (max 10MB)
├── File Type (JPEG, JPG, PNG)
└── File Selection Required

Backend Validation:
├── Content-Type Check
├── File Read Error Handling
├── Model Load Check
└── Database Connection Check

Error Handling:
├── Try-Catch Blocks
├── HTTPException with codes
├── User-Friendly Messages
└── Console Error Logging
```

---

## Deployment Architecture (Future)

```
┌──────────────────────────────────────┐
│          Vercel (Frontend)           │
│       Next.js App Deployment         │
│     https://treesense.vercel.app     │
└───────────────┬──────────────────────┘
                │
                │ API Calls
                ▼
┌──────────────────────────────────────┐
│       Railway/Render (Backend)       │
│      FastAPI + Uvicorn Server        │
│   https://api.treesense.railway.app  │
└───────────────┬──────────────────────┘
                │
                │ Database Queries
                ▼
┌──────────────────────────────────────┐
│         MongoDB Atlas (Cloud)        │
│    Managed MongoDB Database          │
│    mongodb+srv://cluster.net/        │
└──────────────────────────────────────┘
```

---

**This is your complete TreeSense tree detection service architecture! 🌳**
